[pytest]
addopts =
    -p django
    --ds=config.settings

addopts +=
    --cov=api         # Покрываем папку приложения
    --cov-report term-missing  # Показывает пропущенные строчки
    --cov-fail-under=80       # Критерий сдачи курсовой